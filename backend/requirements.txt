# Runtime dependencies for the ContextIQ backend.
# Pinned to the versions this codebase is tested against.

fastapi==0.139.2
uvicorn[standard]==0.51.0
sqlalchemy[asyncio]==2.0.51
alembic==1.18.5
asyncpg==0.31.0
pgvector==0.5.0
pydantic==2.13.4
pydantic-settings==2.14.2
anthropic==0.117.1
# torch is pinned here only to stop the version drifting past the CPU wheel
# installed by requirements-torch.txt. Install that file FIRST -- resolving
# this one alone on Linux pulls torch from PyPI and with it the CUDA stack.
torch==2.13.0
sentence-transformers==5.6.0
pypdf[crypto]==6.14.2
python-multipart==0.0.32
