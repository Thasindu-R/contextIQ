// FileUpload: document upload UI (FR-1).
// Single responsibility: file selection + upload trigger via api/client.
// No ingestion or parsing logic (that's backend-only).

export default function FileUpload(): JSX.Element {
  // TODO: file input + drag-drop; call api/client.uploadDocument on submit.
  throw new Error("Not implemented");
}
