// Vite configuration.
// Single responsibility: build/dev-server config only.

import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";

// TODO: export default defineConfig({ plugins: [react()] });
export default defineConfig({});
